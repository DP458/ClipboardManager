<Page
    x:Class="ClipboardManager.AboutPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ClipboardManager"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" 
    Loaded="Page_Loaded">

    <Page.TopAppBar>
        <CommandBar Style="{ThemeResource TopAppBarStyle}">
            <CommandBar.Content>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Style="{ThemeResource TopBarPageLogoStyle}" Text="&#xE946;"/>
                    <TextBlock x:Name="PageHeader" x:Uid="AboutPageHeader" Style="{ThemeResource TopBarTextBlockStyle}" Text="Information"/>
                </StackPanel>
            </CommandBar.Content>
        </CommandBar>
    </Page.TopAppBar>

    <Page.Content>
        <ScrollViewer Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
            <ScrollViewer.Content>
                <StackPanel Orientation="Vertical">
                    <StackPanel Orientation="Horizontal">
                        <Border Background="{ThemeResource SystemControlBackgroundAccentBrush}" Width="48" Height="48" Margin="15,15,0,0" 
                                VerticalAlignment="Top">
                            <Border.Child>
                                <Border HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Width="34" Height="34">
                                    <Border.Child>
                                        <Image x:Name="Logo"/>
                                    </Border.Child>
                                </Border>
                            </Border.Child>
                        </Border>
                        <TextBlock x:Name="TextBlockDescription" Margin="10,0,3,0" Padding="0,12,12,0" FontSize="16" IsTextSelectionEnabled="False" TextWrapping="Wrap">
                            <TextBlock.Inlines>
                                <Underline Foreground="{ThemeResource SystemControlBackgroundAccentBrush}">
                                    <Underline.Inlines>
                                        <Run x:Name="AppName"/>
                                    </Underline.Inlines>
                                </Underline>
                                <LineBreak/>
                                <Run x:Uid="AboutPageAppVersion" Text="Version"/>
                                <Run x:Name="VersionNumber"/>
                                <LineBreak/>
                                <Run x:Uid="AboutPageAppDeveloper" Text="Developer"/>
                                <Run x:Name="DeveloperName"/>
                            </TextBlock.Inlines>
                        </TextBlock>
                    </StackPanel>
                </StackPanel>
            </ScrollViewer.Content>
        </ScrollViewer>
    </Page.Content>

    <Page.BottomAppBar>
        <CommandBar>
            <CommandBar.PrimaryCommands>
                <AppBarButton x:Name="ButtonBack" x:Uid="AboutPageButtonBack" Icon="Back" Label="Back" Click="ButtonBack_Click"/>
            </CommandBar.PrimaryCommands>
        </CommandBar>
    </Page.BottomAppBar>

</Page>